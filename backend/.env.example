# Copie para .env e ajuste. Nunca versione o arquivo .env.
APP_ENV=development
SECRET_KEY=12345
DATABASE_PATH=instance/romera.db

# Administrador criado automaticamente na primeira execução (se ainda não existir nenhum)
ADMIN_EMAIL=feliphegbatistuta@gmail.com
ADMIN_PASSWORD=B49842370

# Regras do pedido (valores em centavos)
DELIVERY_FEE_CENTS=800
MIN_ORDER_CENTS=3000

# WhatsApp da loja no formato internacional, só números
WHATSAPP_NUMBER=5511975680313

# WhatsApp exclusivo do suporte (widget de ajuda do site). Opcional:
# se deixar vazio, o widget usa o WHATSAPP_NUMBER acima.
SUPPORT_WHATSAPP_NUMBER=5511975680313

# Origens extras permitidas (separadas por vírgula). Vazio = só a mesma origem.
CORS_ORIGINS=
JWT_EXPIRES_MINUTES=480
